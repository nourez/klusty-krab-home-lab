apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: govee2mqtt-data
  namespace: govee2mqtt
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 1Gi
