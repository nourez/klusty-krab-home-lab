apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: audiobookshelf-hardcover-sync-data
  namespace: audiobookshelf-hardcover-sync
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 1Gi
