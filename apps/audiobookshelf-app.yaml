apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
    name: audiobookshelf
    namespace: argocd
spec:
    project: default
    sources:
        - repoURL: ghcr.io/bjw-s/helm
          chart: app-template
          targetRevision: 3.7.3
          helm:
              valueFiles:
                  - $values/manifests/audiobookshelf/values.yaml
        - repoURL: git@github.com:nourez/klusty-krab-home-lab.git
          targetRevision: HEAD
          ref: values
        - repoURL: git@github.com:nourez/klusty-krab-home-lab.git
          targetRevision: HEAD
          path: manifests/audiobookshelf
          directory:
              recurse: true
    destination:
        server: https://kubernetes.default.svc
        namespace: audiobookshelf
    syncPolicy:
        automated:
            selfHeal: true
            prune: true
        syncOptions:
            - CreateNamespace=true
